import 'package:flutter/material.dart';

class InformationContactsScreen extends StatelessWidget {
  const InformationContactsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
